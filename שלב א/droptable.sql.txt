-- Drop the ticket table first because it references seat and Movie
DROP TABLE ticket;

-- Drop the projector_employee table because it references Movie
DROP TABLE projector_employee;

-- Drop the food table (independent)
DROP TABLE food;

-- Drop the seat table (independent)
DROP TABLE seat;

-- Drop the movie table
DROP TABLE movie;

-- Drop the cinema_room table (independent)
DROP TABLE cinema_room;